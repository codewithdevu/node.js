<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>home</title>
</head>
<style>
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
</style>

<body>
    <h1>Url Shortner</h1>
    <% if (locals.id) { %>
        <p> Generated Url: http://localhost:5001/url/<%= id %>
        </p>
        <% } %>
            <div>
                <form method="POST" action="/url">
                    <label>Type Your Orignal Url</label>
                    <input type="text" name="url" placeholder="https://example.com">
                    <button type="submit"> Generate</button>
                </form>
                <table>
                    <thead>
                        <tr>
                            <th>S.No</th>
                            <th>ShortId</th>
                            <th>RedirectId</th>
                            <th>Clicks</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% urls.forEach((url , index) => { %>
                            <tr>
                                <td>
                                    <%= index + 1 %>
                                </td>
                                <td>
                                    <%= url.shortUrlId %>
                                </td>
                                <td>
                                    <%= url.redirectUrl %>
                                </td>
                                <td>
                                    <%= url.urlVisitHistory.length %>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
            </div>
</body>

</html>